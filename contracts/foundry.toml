[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.19"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
anvil = "http://127.0.0.1:8545"

# Anvil's default first private key (only ever used for LOCAL testing).
# Never reuse this key or the funded Anvil accounts on a real network.
